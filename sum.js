module.exports = {
  sum: function (a, b) {
    return a + b;
  },

  testAdd: function (a) {
    return a;
  },
};

// const sum = (a, b) => {
//   return a + b;
// };

// const sume = (a, b, c) => {
//   return a + b + c; 
// };

// exports.sum = sum;
// exports.sume = sume;
